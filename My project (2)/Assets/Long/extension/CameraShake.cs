using UnityEngine;

// Tên lớp PHẢI khớp với tên file
public class CameraShake : MonoBehaviour
{
    // Khởi tạo các hàm và biến ở đây (dù hiện tại có thể trống)
    public void Shake(float duration, float magnitude)
    {
        // ... Logic rung màn hình sẽ ở đây ...
        Debug.Log($"Shaking for {duration} seconds with magnitude {magnitude}.");
    }
}